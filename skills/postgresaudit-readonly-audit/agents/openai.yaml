interface:
  display_name: "PostgresAudit Read-Only Audit"
  short_description: "Guide safe read-only PostgreSQL audits with PostgresAudit"
  brand_color: "#0F766E"
  default_prompt: "Use $postgresaudit-readonly-audit to guide a safe read-only audit with https://postgresaudit.com/."
